# github.com/KoddiDev/pop/v6/slices

This package provides support for datatypes like `JSONB` in postgres

## Installation

``` bash
$ go get github.com/KoddiDev/pop/v6/slices
```

## Supported Datatypes

* `slices.Map` 
* `slices.Int` 
* `slices.Float` 
* `slices.String`
* `slices.UUID`